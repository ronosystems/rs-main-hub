{
  "name": "main-hub",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/src/index.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
