{
  "name": "rs-main-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd MAIN_HUB/frontend && serve -s build -l $PORT",
    "postinstall": "cd MAIN_HUB/frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
