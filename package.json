{
  "name": "rs-main-hub-frontend",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd MAIN_HUB/frontend && npm install && npm run build",
    "start": "cd MAIN_HUB/frontend && npx serve -s build -l $PORT"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
