{
  "name": "tronic-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && serve -s build -l $PORT",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
